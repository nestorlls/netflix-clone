import { styled } from 'styled-components';

const MyListWrapper = styled.div`
  margin-top: 8rem;

  .mylist-title {
    margin-bottom: 4rem;
  }

  .mylist-container {
    gap: 1rem;
    flex-wrap: wrap;
  }
`;

export default MyListWrapper;
