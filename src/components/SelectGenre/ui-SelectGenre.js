import { styled } from 'styled-components';

const SelectGenreWrapper = styled.select`
  cursor: pointer;
  font-size: 1.4rem;
  background-color: rgba(0, 0, 0, 0.6);
  color: white;
`;

export default SelectGenreWrapper;
