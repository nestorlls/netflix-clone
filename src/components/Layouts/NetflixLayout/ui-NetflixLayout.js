import { styled } from 'styled-components';

const NetflixLayoutWrapper = styled.div`
  .container {
    padding-top: 7rem;
    width: 100%;
    height: max-content;
    padding-left: 1rem;
    padding-right: 1rem;
    padding-bottom: 1.2rem;
  }
`;

export default NetflixLayoutWrapper;
